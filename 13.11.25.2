l = []
for N in range(10000):
    R = bin(N)[2:]
    if sum(map(int, R)) % 2 == 0:
        R = "10" + R[2:] + "0"
    else:
        R = "11" + R[2:] + "1"
    if N > 27:
        l.append(int(R, 2))

print(min(l))
